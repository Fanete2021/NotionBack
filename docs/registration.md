# Регистрация

## Сценарий 1. Регистрация

**Что видит пользователь:** Форма входа — поля email, пароль, имя. Кнопка «Зарегистрироваться».

**Что делает:** Вводит email, пароль (минимум 8 символов), имя. Нажимает «Зарегистрироваться».

**Что происходит внутри:** `POST /api/auth/register`. Сервер в одной транзакции:

- Хеширует пароль (bcrypt/argon2).
- Создаёт User.
- Создаёт Workspace с name = `"{имя пользователя}'s workspace"`, ownerId = user.id.
- Создаёт WorkspaceMember с role = OWNER, связывая пользователя и воркспейс.
- Отдаёт access-токен (JWT, 15 мин) + refresh-токен (httpOnly cookie, 30 дней).

**Результат:** Пользователь авторизован. В куке — refresh, в памяти клиента — access. Он сразу попадает в свой новый воркспейс (сценарий 1.5).

**Ошибки:**

- Email уже занят → 409.
- Пароль < 8 символов или невалидный email → 400 (валидация DTO).
- Общие ошибки → формат `{ statusCode, message, error, path, timestamp }`.
