const DEFAULT_IMAGE_MAX_BYTES = 10 * 1024 * 1024; // 10MB
const DEFAULT_VIDEO_MAX_BYTES = 25 * 1024 * 1024; // 25MB
const DEFAULT_PRESIGN_EXPIRES_SECONDS = 300; // 5 minutes
const MAX_ATTACHMENT_SIZE = 10 * 1024 * 1024;

export {
  DEFAULT_IMAGE_MAX_BYTES,
  DEFAULT_PRESIGN_EXPIRES_SECONDS,
  DEFAULT_VIDEO_MAX_BYTES,
  MAX_ATTACHMENT_SIZE,
};
